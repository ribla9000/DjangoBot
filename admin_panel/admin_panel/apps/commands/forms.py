from django import forms
from .models import *

# class CommandsForButtonsForm(forms.ModelForm):

# 	class Meta:
# 		model = CommandsForButtons
# 		fields = (
# 			'command_name',
# 			'command_text'
# 		)
# 		widgets = {
# 			'command_name': forms.TextInput,
# 		}